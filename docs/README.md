# docs

- `scoreboard.html`: the one-page scoreboard for business readers (recall, cost, comparison, roadmap).
- `make_demo.py`: builds `demo.gif`, `dashboard.png`, `questions.png` and `scoreboard.png` from the reference reading of the onboarding example and the frames of a run of it (`out/real2` by default). Run it again after the first real model run so the pictures show real API output.
